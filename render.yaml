services:
  - type: web
    name: care-catalyst
    runtime: python
    plan: free
    buildCommand: pip install -r requirements_demo.txt
    startCommand: uvicorn care_catalyst_fast:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.7
